# 사용법

## 시작과 재개

이 저장소를 작업 공간으로 연 새 대화에서는 [`STATE.md`](./STATE.md)가
유일한 학습 재개 북마크입니다. 다음 문장으로 현재 주강의 범위의 연결된 모듈
하나를 시작합니다.

```text
오늘 학습 시작
```

승인된 같은 과정이나 과제 범위에서 여러 모듈을 이어가고 싶다면 다음 중 하나를
사용합니다.

```text
오늘 전체 학습 흐름 시작
전체 학습 흐름 시작
```

이 요청은 다른 과정이나 새 과제로 자동 진입할 권한이 아닙니다. 승인된 범위가
끝나면 다음 선택지를 제안하고 사용자 결정을 기다립니다.

새 대화에서 이어갈 때는 다음처럼 말합니다.

```text
계속
```

Agent는 `STATE.md`의 `다음 독립 행동`만 재개합니다. `tmp/`의 ignored
파일이나 Notebook metadata에서 과거 phase를 복원하지 않습니다.

멈출 때는 다음처럼 말합니다.

```text
오늘 학습 종료
```

재개 지점이 달라졌다면 Agent가 확인된 근거로 `STATE.md`를
사전 승인 없이 갱신하고 변경 내용을 알립니다.

## 하루 세션 운영

한 세션은 **모듈 하나**입니다. 주제가 바뀌면 세션을 새로 엽니다.

```text
1. 새 세션을 연다
2. "계속"              → STATE.md의 다음 독립 행동부터 재개
3. 공부한다            → 설명 듣고, 직접 구현하고, 실행하고, 해석한다
4. 모듈이 끝나면
   - 목적·원리·가정·한계를 1~2분 무보조로 설명한다
   - 조건을 바꾼 사례 하나를 풀고 한 번의 전체 피드백을 받는다
   - 대화를 닫고 knowledge 초안을 기억으로 먼저 쓴다
   - 대표 구현 단위에 해당하면 빈 파일에서 재구현한다
   - 그 다음에 다시 열어 대조한다
5. "오늘 학습 종료"    → 확인된 재개 지점으로 STATE.md를 갱신한다
6. Agent가 변경 내용을 알리면 필요할 때 정정한다
```

실제 챕터 전환이 확인되면 다음 챕터 전에 `$finish-chapter`로 보관·회고·knowledge·
검증 후 노트북 초기화·로컬 커밋을 기본 수행합니다. 별도 요청이나 재승인은 필요 없습니다.
단순한 `완료`나 `이해했어`, 소단원·세션 종료만으로는 발동하지 않습니다.

중단은 언제든 해도 됩니다. **STATE.md는 확인된 재개 위치에 맞춰 사전 승인 없이 갱신됩니다.**
미실행 계획을 완료로 기록하지 않으며, 위치가 불명확하면 필요한 사실만 질문합니다.
다음에 `계속`으로 다시 시작하면 됩니다.
다만 오래 붙잡고 있던 세션을 끝낼 때는 `오늘 학습 종료`로 재개 지점을 갱신해
두는 편이 다음 세션이 훨씬 싸고 정확합니다.

### 상시로 도는 것

구술·재구현은 해당 Phase의 시간 예산 안에서 진행합니다. 자세한 범위는
[`ROADMAP.md`](./ROADMAP.md), 정적인 개념별 설명 기준은
[`CURRICULUM.md`](./CURRICULUM.md)에 있습니다. 점수표나 회상 추적 시스템은 만들지 않습니다.

- 대표 단위의 **빈 파일 재구현** — P0 scalar autodiff·MLP 학습, P1 선형·로지스틱 회귀·PCA·k-means·교차검증, P2 PyTorch 학습 루프·attention, P3 Transformer block·causal mask, P4 KV cache·추론 측정
- 부족한 대표 구현을 보강하는 **deep-ml**·시간 제한 구현·알고리즘 연습 — 별도 주간 문제 수를 졸업 조건으로 추가하지 않음
- 과정과 연구 질문에 필요한 **논문** 읽기 — P5 재현은 추론 프로젝트 하나에 통합
- 주 1회 **지난주 개념 2개와 더 이전 개념 1개 무보조 구술** — 예고 없이 묶어 확인하되 일반 수업을 매번 진단으로 시작하지 않음
- Phase 끝에 **전환 점검** — 산출물·설명·재구현·논문·시간·직무·범위

전체 모듈의 코드나 공식 과제 전체를 다시 작성하는 의무는 없습니다. 공식 과제
수행과 무보조 재구현은 별개의 근거이며, 도움받은 구현을 무보조 성공으로
기록하지 않습니다. 재구현 중에는 코드·뼈대·import 목록을 제공받지 않습니다.

52주 달력에서 48개 실효 학습주 × 주 60시간 = 2,880시간을 초기 예산으로 두고,
4주는 달력 여유로 남깁니다. 핵심 과제나 설명의 깊이가 부족하면 이후 일정을
조정합니다. Kaggle은 P1만 필수이며, P0에서는 예측을 만들 수 있으면 첫 제출을
제안할 수 있습니다. 이전 제출 경험은 필요하지 않습니다. P2·P3 참여는 핵심
학습 이후 선택입니다. 실제 후보의 데이터 규모·연산량·기간·라이선스는 선택할 때 확인합니다.

## 모델과 reasoning 강도 고르기

**Phase나 챕터가 아니라 그 순간 하는 일**로 정합니다. `P0`의 막힌 디버깅이
`P4`의 개념 설명보다 더 깊은 추론을 필요로 합니다. 모델 이름과 가격은 자주
바뀌므로 이 문서에는 등급만 적고, 실제 이름은 쓰는 시점에 각 제공자 문서에서
확인합니다.

| 하는 일 | 등급 | reasoning |
|---|---|---|
| 환경·설치·경로 확인 같은 잡일 | 가장 싼 것 | low |
| 빈 파일 재구현 결과 대조 | 낮음~중간 | low~medium |
| 개념 설명 듣기 | 중간 | medium |
| 논문 요약의 오독 지적 | 중간 | medium |
| 내 답·코드 피드백 | 중간 | medium~high |
| Kaggle error analysis | 중간~높음 | high |
| **수식 유도 검증** (CS229·CS224N written) | 높음 | high~xhigh |
| **막힌 디버깅** | 높음 | high~xhigh |
| **커널·성능 디버깅** (`P4`) | 높음 | xhigh |
| **실험 설계·논문 재현 검토** (`P5`) | 높음 | xhigh |

기본값은 **중간 등급 + medium**으로 두고 필요할 때만 올립니다. 가장 비싼
모델을 낮은 reasoning으로 쓰는 조합은 피합니다. 비싼 값을 치르면서 그 값을
하게 만드는 부분을 끄는 셈입니다. 비싼 모델이 필요하면 reasoning도 같이
올리고, 아니면 등급을 내립니다.

Phase가 진행될수록 높은 등급의 비중이 늘어납니다. `P0`~`P1`은 대부분 중간
등급으로 충분하고, `P4`~`P5`는 절반 가까이 높은 등급이 필요합니다. 설명을
듣는 시간이 줄고 직접 구현하다 막히는 시간이 늘기 때문입니다.

## AI 도구를 두 개 쓸 때

두 개를 같이 써도 됩니다. 운영 규칙이 도구 설정이 아니라 **파일에** 있기
때문입니다. `AGENTS.md`가 원본이고 `CLAUDE.md`는 그 심볼릭 링크라, 어느 쪽을
열어도 같은 규칙을 읽습니다.

지켜야 할 것은 네 가지입니다.

- **`STATE.md`가 유일한 인수인계 지점입니다.** 대화 기록, 도구별 메모리,
  개인 설정은 넘어가지 않습니다. 새 세션에서는 "아까 얘기한 거"에 의존하지
  말고 파일에 있는 것만 근거로 씁니다.
- **동시에 두 개를 돌리지 않습니다.** 같은 `STATE.md`를 두 도구가 고치면
  북마크가 해결할 수 없는 충돌이 납니다. 하나 끝내고 다음을 엽니다.
- **두 번째 도구는 답을 받아낼 두 번째 기회가 아닙니다.** 한쪽이 연습 코드를
  안 써 주니 다른 쪽에 물어보는 순간, 규칙이 아니라 본인의 검증 장치가
  무너집니다.
- **설명을 교차 검증하는 것은 권장합니다.** 한쪽에 설명을 듣고 다른 쪽에
  반박을 시켜 보면 이해가 단단해집니다. 두 도구가 엇갈리면 더 자신 있는 쪽이
  아니라 **공식 자료**가 결정합니다.

비용 측면에서는 무거운 쪽을 기본값으로 두지 말고, 막힌 디버깅이나 엄밀한
추론 검토가 필요할 때만 올려 쓰는 편이 낫습니다.

## 다른 컴퓨터에서 이어가기

이동 전 `main.ipynb`를 저장하고 Agent가 현재 재개 지점을 `STATE.md`에
반영하도록 합니다. 챕터 중간 이동에는 노트북 초기화나 챕터 마무리가 필요 없습니다.
최신 노트북과 갱신된 상태를 명시적 요청으로 commit·push한 뒤, 다른 컴퓨터에서
해당 branch의 변경을 받습니다. 미커밋 변경과 ignored 파일은 clone에 포함되지 않습니다.

새 컴퓨터에 Git, uv, 사용할 AI 코딩 도구와 Notebook 실행 도구를 준비하고 로그인합니다.
어느 도구를 쓰든 저장소의 `AGENTS.md`·`CLAUDE.md`가 같은 규칙을 전달합니다.
저장소 루트에서 `uv sync --frozen`을 실행하고 Notebook kernel로 새 환경의
`.venv` Python을 선택합니다. Python 버전은 `.python-version`과 `pyproject.toml`을
따릅니다. 기존 컴퓨터의 `.venv`를 복사하지 않습니다. OS별 설치·실행 가능 여부는
새 컴퓨터에서 확인해야 하며, lockfile만으로 모든 환경의 동일 결과를 보장하지 않습니다.

새 세션에는 다음처럼 요청합니다.

```text
AGENTS.md와 STATE.md를 읽고 현재 학습을 이어줘. main.ipynb는 통째로 읽지 말고
scripts/nbpeek.py로 필요한 셀만 봐줘. 새 커널이므로 필요한 셀의 의존관계부터
확인하고, 기존 학습 셀을 무조건 전체 재실행하지 말아줘. 실행은 내가 할게.
```

저장소는 대화 전문이나 개인 도구 설정·전역 스킬을 전달하지 않습니다.
저장소의 운영 지침과 `.agents/skills/`는 파일로 전달되며 현재 위치는 `STATE.md`로
재개합니다. 인증 정보나 개인 도구 설정 폴더를 Git에 추가하지 않습니다.

Notebook은 저장된 코드·출력을 보존하지만 커널의 변수, 학습된 가중치,
난수 생성기 상태를 복원하지 않습니다. 새 커널에서는 현재 행동에 필요한
데이터·모델 구성 셀을 확인한 뒤 학습자가 실행합니다. 과거 학습을 반복한 횟수나
배치 상태가 남아 있지 않으면 과거 수치의 정확한 재현은 미확인입니다.
기존 학습 모델을 정확히 이어야 한다면 이동 전에 별도 checkpoint 저장 범위를
정해야 합니다. 저장 출력은 checkpoint가 아닙니다.

## 토큰 비용을 줄이며 쓰기

대화형 학습은 매 턴 대화 전체를 다시 보내므로 비용이 턴 수의 제곱으로 늘어납니다.
아래 습관이 실제로 효과가 큽니다.

- **모듈 하나에 세션 하나.** 주제가 바뀌면 세션을 새로 시작합니다. 하루 종일
  한 세션을 끌면 뒤쪽 턴 하나가 앞쪽 턴 수십 개보다 비쌉니다.
- **질문을 묶어서 한 번에.** 15분 간격으로 세 번 묻는 것보다 세 개를 한 번에
  묻는 쪽이 훨씬 쌉니다.
- **설명·채점은 작은 모델로.** 정말 막힌 디버깅이나 엄밀한 추론 검토가 필요할
  때만 큰 모델로 올립니다. 기본 설정을 가장 비싼 조합으로 두지 않습니다.
- **Notebook을 통째로 읽히지 않습니다.** 저장된 그림이 base64로 딸려 오므로
  보관본 하나가 17만 토큰을 넘기도 합니다.

```bash
python3 scripts/nbpeek.py main.ipynb --list        # 셀 목록만
python3 scripts/nbpeek.py main.ipynb --cells 3-7   # 필요한 셀만
python3 scripts/nbpeek.py main.ipynb --no-output   # source만
```

Agent에게도 같은 규칙이 `AGENTS.md`에 있습니다. Notebook을 직접 열어 읽으려
하면 `nbpeek`을 쓰라고 지적해 주세요.

## 한 모듈의 진행 방식

기본 수업은 다음 흐름을 사용합니다.

```text
기준 자료·판본 → 직접 링크 → 이번 설명 범위 → 수행할 공식 실습 → 확인할 결과
→ 영상·문서·공식 자료 기반 대화로 학습
→ 공식 구현·exercises·assignments의 직접 수행과 결과 해석
→ 맞은 점, 고칠 점, 이유, 빠진 핵심을 한 번에 피드백
```

질문에 필요한 Tensor 값, shape, dtype, device, 데이터 분리, 평가 목적은
같은 메시지 안에 모두 적습니다. 단순 계산이 학습 목표가 아니면 계산은
Agent가 채우고 개념과 해석에 집중합니다. 이미 정확히 설명한 내용을
다른 표현으로 반복 시험하지 않습니다. 내부 routing label, ID, review tag,
metadata는 사용자가 요청하지 않는 한 채팅에 표시하지 않습니다.

모듈 끝의 무보조 설명에서는 학습자가 Tensor shape와 모델 흐름도 직접
제시합니다. 이 회상 구간에만 위 조건 제공 규칙의 예외가 적용됩니다.
AI는 면접 답변집이나 knowledge 초안을 먼저 써 주지 않고 학습자의 초안을
공식 자료와 대조해 교정합니다.

막혔을 때는 현재 파일 경로와 실제 오류 또는 출력을 함께 지정하면
좋습니다.

```text
practice/<area>/<file>을 보고 지금 난 오류 원인부터 설명해줘.
```

Agent는 저장된 코드와 실제 출력에서 첫 blocker를 확인합니다. 별도 요청
없이 학습자 구현을 덮어쓰지 않습니다.

오류가 발생했다면 수정 전에 세운 첫 원인 가설과 이를 확인한 방법을
설명합니다. 오류가 없으면 오류 가설을 만들 필요가 없습니다.

주강의의 순서와 자료별 역할은 [학습 로드맵](./ROADMAP.md)을 따릅니다.
학습 시작은 새 준비도 진단이나 로드맵 검토가 아니라 현재 주강의 단원으로
연결합니다. 영상·문서·공식 자료 기반 대화를 허용하며, 설명 매체를 바꾸어도
공식 내용과 실습을 축소하지 않습니다. 강의 구간을 확인할 수 없다면 그 사실을 알리고 관련
발췌나 시청 위치를 요청합니다. 확인하지 않은 내용·시각은 기록하지 않습니다.

현재 설명에 필요한 선수개념을 충분히 보강한 뒤 같은 주강의로 돌아갑니다.
보강 횟수를 제한하지 않으며, 공식 API 문서를 참고하면서 핵심을 재구성해도
됩니다. 자동미분 엔진 전체를 암기해 재작성하도록 요구하지 않습니다.
KANT는 진도·주제 대조용이며 기본 실습이나 완료 기준으로 사용하지 않습니다.
micrograd 강의 속 전체 구현은 수행하되 전체를 암기해 재작성하는 별도
통과 조건을 요구하지 않습니다. 대표 단위인 scalar autodiff와 MLP 학습 흐름은
빈 파일에서 확인하되, 전체 엔진 암기나 새 과제 진입 시험으로 확대하지 않습니다.
보조 레퍼런스는 필요한 범위만 사용합니다.

공식 실습을 배치할 때 문제 본문을 읽고 필수와 Optional·Bonus를 구분합니다.
접근·실행 제약이 있는 항목은 미완료로 남기며 대체 문제로 완료 처리하지
않습니다. 대화형 학습은 영상 시청으로, 로컬 검사·리뷰 통과는 대학의 공식
채점 통과로 기록하지 않습니다.

## STATE.md 반영

`STATE.md`에는 공개 가능한 최소 정보만 둡니다.

- 확인된 pilot 날짜
- 현재 `ROADMAP.md`의 정적 Phase ID 하나 (`P0`~`P5`)
- 현재 주강의·정확한 강의 제목·현재 범위
- 연결 실습과 보조 자료
- 이미 존재하는 관련 artifact의 짧은 사실
- 다시 확인할 항목
- 다음 독립 행동 하나

답변 원문, private 경로, 내부 ID, hash, 점수표, 세션 이력, metrics는
넣지 않습니다. 공개 source를 고정하는 commit은 예외입니다. Phase ID는
정적인 위치 참조일 뿐이며 비율·점수·준비도 판단·누적 시간·완료 체크·자동
다음 목표를 붙이지 않습니다. Phase 변경은 ROADMAP 전환 점검과 기존 과정 선택 규칙을 따릅니다.

재개 지점이 바뀌면 Agent가 학습자의 설명과 확인한 artifact를 근거로
사전 제안이나 승인 없이 갱신하고 변경 내용을 알립니다. `STATE 반영해`를
별도로 말할 필요는 없습니다. 미실행 계획과 확인되지 않은 이해를 완료로
기록하지 않으며, 새 과정 진입이나 순서 변경 권한으로 확대하지 않습니다.

자동 또는 요청에 따른 STATE 갱신은 `STATE.md` 수정만 허용합니다.
commit과 push는 각각 별도의 명시적 요청이 필요합니다. 파일이 없거나 실제
artifact와 충돌하면 확인된 사실로 복구하고, 불명확한 사실만 질문합니다.

## 공식 과제와 공개 저장소

과정의 판본·필수 범위는 [`ROADMAP.md`](./ROADMAP.md)를 따릅니다. MIT 18.05
Spring 2022 PS1~PS11의 R 요구사항, CS229 Summer 2020 PS1~PS3의 필수
written·coding, CS231n Spring 2024 A2 Q1~Q5를 유지하며 보조 재구현으로
공식 과제를 대체하지 않습니다. CS231n Lecture 2~6은 공개 notes·slides가
기본 설명 자료이며 A1·A3·final project는 범위 밖입니다.

MIT 18.05 문제 세트 답안과 CS229·CS231n·CS224N·CS336 공식 과제 답안은
written·코드·노트북·저장 출력 모두 **별도 비공개 작업 공간**에 둡니다.
공개 저장소에는 공식 문제 원문·과제 답안·저작권 자료를 넣지 않습니다.
학습자 자신의 개념 노트, 공식 답안과 분리된 빈 파일 재구현, 공개 가능한
대회 결과와 재현 보고서를 남깁니다. 자습 과제에도 같은 경계를 적용합니다.
새 자료 확보·환경 설치는 해당 학습 진입 시 별도 요청과 공식 요구사항에 따라
수행하며 현재 환경에 R이나 과제 의존성을 자동 추가하지 않습니다.

## CS224N의 수행 범위와 도움

CS224N Spring 2024 A1~A4는 `P3`에서 written·수학·프로그래밍 요구사항까지
전부 수행합니다. [공식 AI 정책](https://web.stanford.edu/class/archive/cs/cs224n/cs224n.1246/index.html)은
AI 협업을 허용하지만 직접 답 요구·복사와 AI의 실질적 과제 대행을 금지합니다.
개별 과제의 별도 지침도 확인합니다.

파이널 프로젝트만 보류이며 `P5`의 추론 최적화 프로젝트가 대신합니다.
완료가 아니라 미완료 보류이고 복귀 조건은 [`DEFERRED.md`](./DEFERRED.md)에
있습니다. 변경·생략은 사용자 결정으로만 합니다.


## CS336 과제에서 받을 수 있는 도움

[Stanford CS336 Spring 2026](https://cs336.stanford.edu/) Assignment 1 기준은
[`a158843b20107949f1a8d7df1b05cd33b9166712`](https://github.com/stanford-cs336/assignment1-basics/tree/a158843b20107949f1a8d7df1b05cd33b9166712)입니다.
현재 주강의가 바뀌어도 기존 과제 작업을 보존합니다. 별도 요청 없이 과제를
clone하거나 다운로드하지 않습니다. 복귀 근거는 로드맵을 따르며, 사용자
승인 없이 현재 과정을 바꾸지 않습니다.

기초 실습은 learning-lab의 Python 3.14 환경에서 실행합니다. 실제
Assignment 1은 별도 sibling clone과 공식 pyproject에 맞춘
Python 3.12 또는 3.13의 독립 uv 환경을 사용합니다. 과제를 learning-lab의
dependency로 추가하거나 현재 `.venv`에 설치하지 않습니다.

과제에 진입한 뒤에는 공식 AI 지침을 그대로 적용합니다.

- 학습자가 과제 코드를 직접 작성하고 제공된 테스트를 실행합니다.
- 학습자가 모든 bash command를 직접 실행하며, Agent는 assignment repo에서
  command를 실행하지 않습니다.
- Agent는 개념 설명, 오류 메시지 해석, sanity check, 일반적 리뷰만
  제공합니다.
- 공식 handout에 이미 나온 command의 의미와 학습자가 제공한 실행 결과는
  설명할 수 있지만, 과제 해결·자동화를 위한 새로운 command sequence는
  만들지 않습니다.
- 코드, pseudocode, patch, TODO 해답은 명시적으로 요청해도 제공하지
  않습니다.

## Practice, TIL, knowledge

### 챕터 전환 시 기본 마무리

```text
$finish-chapter
이번 챕터 정리해줘
```

위 요청 또는 실제 챕터 전환은 [챕터 마무리 스킬](./.agents/skills/finish-chapter/SKILL.md)의
실습 보관·챕터 회고·knowledge 갱신·검증 후 작업 노트북 초기화·로컬 커밋까지
허용합니다. 챕터 전환은 이미 부여한 기본 권한이며 매번 요청하지 않습니다.
입력은 현재 대화와 저장된 `main.ipynb`, 해당 챕터의 `recall.ipynb` 등입니다.
과정과 실제 산출물로 챕터 경계를 확인하며 미완료 요구사항은 숨기지 않습니다.
`완료`, `이해했어`, `오늘 학습 종료`에는 자동 실행하지 않습니다.

- `practice/<area>/<chapter>.ipynb`: 코드·출력·메타데이터를 그대로 보존한 원본.
- 같은 이름의 `.md`: 공식 수행 범위, 질문·수정·도움, 결과와 한계, 개념 링크.
- `knowledge/`: 확인된 재사용 개념을 기존 문서부터 갱신. 챕터 정리는 3개 제한이 없습니다.

knowledge 교정 전에 학습자의 초안을 확인합니다. 대화에서 학습자가 직접 쓴
개념 설명도 초안으로 사용할 수 있으므로 중복 작성을 요구하지 않습니다.
단순 이해 응답이나 AI 설명은 초안이 아니며 도움받은 범위를 구분합니다.
학습자 근거가 없는 개념은 회고의 미확인 사항으로 남기고 knowledge에 올리지 않습니다.

저장된 노트북은 재실행하거나 고치지 않습니다. 회고·knowledge와 링크 검증 후
모든 대상 보관본의 일치를 재확인하고, 각 작업 노트북에 메타데이터와 빈 코드 셀 하나만 남깁니다.
미저장 편집과 커널 상태는 보관 대상이 아니므로 마무리 전에 저장하고,
초기화 중에는 동시에 편집·저장하지 않습니다. 보관 실패나 파일 변경을 발견하면
원본을 유지합니다. 기존 보관본이 다른 내용이면 덮어쓰지 않습니다.

마지막에 확인된 근거로 STATE를 갱신하고 최종 검사와 이미 허용된 로컬 커밋까지
추가 확인 없이 진행합니다. STATE 갱신 자체는 파일 수정 권한이며, 커밋 권한은
명시적 챕터 마무리 요청 또는 챕터 전환의 기본 권한에서 온 것입니다. STATE가 이미 정확하면 그대로 둡니다.
무관한 기존 변경은 별도 허용 없이 넣지 않습니다.
push, 날짜별 TIL, 다음 강의 진행은 별도 요청입니다.

### 개별 파일 요청

공식 구현·exercises·assignments가 기본 실습입니다. 강의 속 구현은 설명을
따라 작성·실행하고, 별도 문제는 직접 시도합니다. 강사의 완성 노트북은
참고 자료이며, 완성 노트북 실행이나 AI 보충 예제로 공식 실습을 대체하지
않습니다. 공식 요구사항에 대한 답·코드·실행·해석을 확인하며, 공식 문제가
통합 checkpoint 역할을 할 수 있습니다. 매 구간마다 새 시험·보고서나
metadata Notebook을 만들지 않습니다. 파일을 저장할 때는 명시적으로 요청합니다.

```text
practice/deep-learning/<topic>.py에 이 실험 뼈대를 만들어줘.
```

CS336 과제에서는 위와 같은 코드 생성 요청도 공식 제한 때문에 수행하지
않습니다. 기존 Notebook의 historical metadata는 실행에 영향을 주지 않는
기록으로 보존하며 현재 학습 상태로 사용하지 않습니다.

날짜별 TIL은 명시적으로 요청할 때만 작성합니다.

```text
오늘 대화에서 내가 설명한 내용과 실행한 결과로 오늘 TIL 초안을 만들어줘.
```

대화, draft, 또는 artifact를 정확히 지정해야 합니다. Agent 설명만으로
이해를 꾸미거나 다른 기록을 자동 수집하지 않습니다. `$save-today-til`은
이 standalone 작업을 명시적으로 고정하고 싶을 때만 사용합니다.

Knowledge도 확인된 입력을 지정할 때만 갱신합니다.

```text
내 설명과 practice/<exact-path>의 실행·해석을 바탕으로 knowledge 갱신을 제안해줘.
```

`$update-learning-knowledge`는 이 작업을 명시적으로 고정하는 opt-in
유틸리티입니다. 새로 확인된 durable understanding이 없으면 `NO_CHANGE`가
정상입니다. TIL과 knowledge 파일 수정은 commit을 자동 허용하지 않습니다.

## 자료와 Python 환경

Private 자료는 `materials/private/<course>/`에 두고 공개 저장소에 올리지
않습니다. 강의 제공 실습은 해당 course 아래
`course-provided-practice/`에 보존하고 학습자 산출물인 top-level
`practice/`와 섞지 않습니다.

Notion이나 PDF 원본을 정리할 때는 본문, 토글, 코드 들여쓰기, 출력, 표,
링크, 수식, 그림과 PDF 페이지를 확인합니다. 변환 무결성이 불확실하면
원본을 삭제하지 않습니다.

```bash
cd /home/jake/workspace/learning/llm-research-learning-lab
uv sync
uv run python path/to/script.py
```

새 dependency는 `uv add`로 관리합니다. VS Code Notebook kernel은 이
저장소의 `.venv/bin/python`을 사용합니다. 실행하지 않은 결과는 기록하지
않고, dataset·model weight·credential·큰 생성물은 명시적 승인 없이 Git에
추가하지 않습니다.

## 저장 권한

학습 시작, 재개, 종료, `STATE 반영해`, 단독 TIL 작성, 단독 knowledge 갱신은
commit이나 push를 암묵적으로 허용하지 않습니다. 확인된 챕터 전환의 기본 마무리와
`$finish-chapter`는 위에서 정의한 로컬 커밋까지 포함하며, push는 포함하지 않습니다. 개별 작업은 다음처럼 명시합니다.

```text
변경 파일만 검토해줘.
이 파일들을 commit해줘.
방금 commit을 push해줘.
```

Commit 요청은 push 권한이 아닙니다. Agent는 unrelated 변경을 보존하고,
승인된 exact path만 stage한 뒤 staged diff를 확인합니다.
