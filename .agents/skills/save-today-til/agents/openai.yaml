interface:
  display_name: "오늘의 TIL 저장"
  short_description: "학습자 증거로 TIL을 구성해 날짜별 파일 하나만 저장·커밋합니다"
  default_prompt: "$save-today-til을 사용해 완료된 reviewed lesson의 학습자 증거를 자연스러운 TIL로 구성하고, mixed 내용만 같은 흐름에서 검토한 뒤 날짜별 TIL 하나만 커밋해줘."
policy:
  allow_implicit_invocation: true
