interface:
  display_name: "학습 로드맵 플래너"
  short_description: "순서화된 endpoint 경로에서 실제 다음 target을 읽기 전용으로 결정합니다"
  default_prompt: "$plan-roadmap-learning을 사용해 endpoint와 실제 다음 primary target을 구분하고, 필요한 inline bridge와 정확한 다음 artifact를 정해줘."
